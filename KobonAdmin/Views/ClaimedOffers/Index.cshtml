@model IEnumerable<KobonAdmin.Models.ClaimedOffer>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/_ViewStart.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClaimedTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClaimedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReceiptId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClaimOfferId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClaimedTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClaimedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReceiptId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClaimOfferId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClaimedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClaimedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClaimedId })
        </td>
    </tr>
}

</table>
