@model IEnumerable<KobonAdmin.Models.StoreReceiptDetail>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/_ViewStart.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfStore)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fields)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfStore)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fields)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StoreReceiptId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StoreReceiptId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StoreReceiptId })
        </td>
    </tr>
}

</table>
